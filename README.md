# dalibo/buildpack-qemu

This docker image includes tooling for building VM image in CI:

- qemu.
- packer.
- guestfstools (and a linux kernel for it).
- tini, wait-for-it.sh, git, sudo, make and other tools for CI execution.
